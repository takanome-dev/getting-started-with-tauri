import React from "react";
import { Text } from "@mantine/core";

const Home = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>
        Yay, this is the home page! Finally we can start building our app 🎉
      </Text>
    </div>
  );
};

export default Home;
